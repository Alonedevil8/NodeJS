extends layout/app

block contenido
  section(class="py-5")
    h2(class="text-center text-4xl font-extrabold")= pagina

    div(class="grud grid-cols-1 lg:grid-cols-3 gap-5 mt-14")
      if propiedades.length
          each propiedad in propiedades
            p(class="py-5") 
            include ./templates/card
      
      else 
        p(class="text-center text-gray-800 text-sm py-10") No Hay Resultados
        
  <p>






      